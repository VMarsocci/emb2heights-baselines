name: emb2heights
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - matplotlib
  - tqdm
  - pip:
    - torch>=2.0
    - torchvision
    - rasterio
    - scikit-learn
    - tqdm
